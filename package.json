{
  "name": "@hyas/themes-doks",
  "description": "Hyas Doks theme",
  "version": "0.1.2",
  "browserslist": [
    "defaults"
  ],
  "repository": "https://github.com/h-enk/hyas-themes-doks",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean:install": "shx rm -rf package-lock.json node_modules",
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "auto-changelog -p && git add CHANGELOG.md"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.2"
  },
  "devDependencies": {
    "auto-changelog": "^2.4.0",
    "shx": "^0.3.4"
  }
}
